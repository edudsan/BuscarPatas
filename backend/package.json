{
  "name": "backend",
  "version": "1.0.0",
  "description": "API Node.js com Express e PostgreSQL (Prisma)",
  "main": "index.js",
  "scripts": {
    "start": "nodemon src/server.js", 
    "start:prod": "node src/server.js",
    "build": "npx prisma generate", 
    "seed": "node prisma/seed.js", 
    "migrate:deploy": "npx prisma migrate deploy", 
    "deploy": "npm run build && npm run migrate:deploy && npm run seed", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "express",
    "prisma",
    "postgresql"
  ],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "bcryptjs": "^3.0.2",
    "cloudinary": "^2.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "prisma": "^6.16.2"
  }
}
